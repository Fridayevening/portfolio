# NewBoy frontend — environment template. Copy to `.env.local` for local overrides.

# Backend API origin without `/v1`. This value is embedded at build time.
# Unset in a production build → offline mode (the local engine takes over).
# Production example: https://newboy-api.onrender.com
NEXT_PUBLIC_API_URL=http://localhost:3031
