# NB-001 Handoff

- Owner: Codex
- Branch: `main` working tree
- Base commit: `52193df`
- Status: done
- File scope: repository collaboration documentation only

## Changed

- Added shared cross-agent rules in `AGENTS.md`.
- Added task ownership and status tracking in `TASKS.md`.
- Reduced the root `HANDOFF.md` to a commit-based current snapshot.
- Moved stable technical information into `docs/ARCHITECTURE.md`.
- Recorded accepted i18n, data-integrity, and Hotaru source decisions under `docs/decisions/`.
- Added task-handoff guidance under `docs/handoffs/`.
- Added Copilot-specific repository instructions without duplicating the full shared rule set.
- Aligned the root `README.md` with the verified Python 3.12 environment and required Node.js 24 baseline.
- Clarified task locks, the integration-owner role, and the difference between local commits and remote pushes.

## Verification

- `git status --short`: reports only `HANDOFF.md` and `README.md` as modified, plus the new `.github/`, `AGENTS.md`, `TASKS.md`, and `docs/` paths as untracked.
- `git diff --check`: passed with no whitespace errors.
- A credential-pattern scan across `AGENTS.md`, `HANDOFF.md`, `TASKS.md`, `README.md`, `MIGRATION-PLAN.md`, `.github/`, and `docs/` returned no credential-shaped values after excluding this verification record's literal pattern text.
- `find AGENTS.md HANDOFF.md TASKS.md .github docs -type f`: confirmed that every collaboration-document reference resolves to an existing path.
- Scope review confirmed no changes under `frontend/src/`, `server/src/`, or `server/python/`.
- `server/.venv/bin/python --version`: returned Python 3.12.3.
- A clean VS Code Copilot/DeepSeek context identified the root and directory-level instruction files without an additional file search and correctly summarized the main safety boundaries.
- Repository tracking review confirmed that `.env`, the original ZIP files, virtual environments, dependency directories, and generated data are not tracked.

## Remaining risks

- The documentation changes are uncommitted and have not been pushed.
- The portfolio metrics were already public, but their evidence still needs confirmation before they are migrated into the rebuilt site.
- The current non-interactive shell reports Node.js 23.6.0. Switch to Node.js 24 LTS before running application builds or development servers.

## Reviewer notes

- Copilot instruction loading is confirmed for the current VS Code configuration.
- Use sequential implementation and cross-review by default. Create separate worktrees only when tasks have non-overlapping file scopes and genuinely need to run in parallel.
- Yanfei accepted the workflow on 2026-09-17 and authorized proceeding through the ordered content, implementation, staging and production phases. External deployment actions still require the phase-specific approval gates recorded in `TASKS.md`.
