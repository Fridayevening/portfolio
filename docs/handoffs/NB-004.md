# NB-004 Handoff

- Owner: Codex
- Base commit: `52193df`
- Status: done
- File scope: approved Work and Research frontend content and presentation

## Completed

- Added typed bilingual static content for four Work cases and two Research studies.
- Added Work and Research desktop folders, Start-menu entries and hidden-app settings.
- Added dedicated portfolio reader windows with metrics, structured sections and approved Figma links.
- Changed the no-cookie language default to English while preserving the visitor's saved `nb-lang` choice.
- Added bilingual runtime titles so open portfolio windows and taskbar entries switch language immediately.
- Replaced the global “every number is made up” note with a bilingual distinction between playful desktop simulations and evidence-reviewed portfolio claims.

## Verification

- `npx tsc --noEmit --incremental false`: passed.
- `npm run build`: passed with Next.js 16.3.4.
- Browser smoke check: English default, Work and Research navigation, case rendering, Figma link, immediate language switching and cookie persistence passed.
- Full-repository `npm run lint` remains blocked by pre-existing React lint errors in `Ledger.tsx`, `Melt.tsx`, `Paperclip.tsx` and `windows.tsx`; the NB-004 files introduced no reported lint error.
- A browser-only MetaMask connection warning appeared from the installed extension, not from NewBoy.

## Implementation decision

The first release keeps public portfolio content in typed frontend data. It is reviewed, read-only content, so adding a database collection and runtime editor would increase deployment and failure surface without a release requirement. The structure can be migrated behind an API later without changing the reader UI.
