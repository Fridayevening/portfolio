# NB-005 Handoff

- Owner: Codex
- Base commit: `52193df`
- Status: review
- File scope: deployment configuration, runtime security, CI and operations documentation

## Implemented

- Added a Node 24 + Python Docker image for the NestJS API and Hotaru dependencies.
- Added a Render Blueprint with manual deployment, health checking and secret placeholders.
- Added Vercel framework configuration for the frontend.
- Restricted production CORS to `CORS_ORIGINS`; local development allows only localhost port 3030.
- Added proxy-aware client IP handling, request validation hardening, common security headers and public host binding.
- Added Node 24 GitHub verification for frontend type/build and server build.
- Documented the selected topology, environment matrix, staging checks, production gate, persistence limitation and rollback path.

## Verification

- Server `npx tsc --noEmit --incremental false`: passed.
- Server JavaScript emission to a temporary directory: passed.
- Frontend type check and production build passed under NB-004.
- A local Docker build was not run because Docker is not installed on this machine.
- Starting the emitted server reached Nest application startup, but the sandbox denied binding a local test port. CI runs both the full Node 24 build and a Docker image build.

## Review request

Copilot should review:

1. `server/src/main.ts` CORS, proxy and header behavior.
2. `server/Dockerfile`, `.dockerignore` and `render.yaml` paths and production assumptions.
3. `.github/workflows/verify.yml` monorepo commands.
4. `docs/DEPLOYMENT.md` for missing provider or rollback steps.
5. The entire NB-004 content/UI diff for unsupported claims, language gaps and accessibility regressions.

No service, account, database, secret, commit, push or deployment has been created.

## Review resolution

- Copilot review received on 2026-09-17.
- Replaced the Debian Bookworm Python 3.11 runtime with an official Python 3.12 base plus the official Node 24 binary and npm modules. NumPy 2.5.2 requires Python 3.12 or newer.
- Corrected the requirements comment to the verified Python 3.12.3 environment.
- Set the Render staging plan explicitly to `free`; omitted plans default to paid `0.5c-512mb` compute.
- Moved security headers before CORS preflight handling and documented the one-hop Render proxy assumption.
- Localized the portfolio kind label and added shared Enter/Space activation for folder items while preserving custom key handlers such as F2 rename.
- Docker construction still requires CI or a Docker-capable machine before NB-006 can be marked ready.
